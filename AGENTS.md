# AGENTS.md

> SpendGuard 저장소 공통 작업 규칙

## Scope

- 사용자의 현재 명시적 지시를 가장 우선한다.
- 기능 범위 판단이 필요할 때 `.project/plan.md`를 확인한다.
- 현재 작업은 해당 Phase의 `docs/instructions/` 문서를 기준으로 제한한다.
- 현재 Phase 밖 기능, 의존성, 권한, 운영 정책을 선반영하지 않는다.
- 자료에 없는 요구사항·경로·명령·결과를 만들지 않는다.
- `DESIGN.md`는 UI 기준이며 기능 범위의 최종 기준으로 사용하지 않는다.

## Work

- 필요한 파일만 확인하고 이미 확인한 내용의 재탐색을 피한다.
- 기존 코드와 문서를 먼저 확인한 뒤 최소 범위로 변경한다.
- 현재 Phase에 필요하지 않은 추상화와 공통화는 추가하지 않는다.
- 설정값과 비밀값은 가능한 범위에서 환경 변수로 관리한다.
- 계산 로직은 LLM 자연어 추론이 아니라 결정론적 코드로 구현한다.
- 외부 사실과 계산 결과를 임의로 생성하지 않는다.

## Verification

- 변경 범위와 직접 관련된 테스트를 실행한다.
- 계산 로직은 고정 입력과 예상 결과를 기준으로 검증한다.
- 실패 결과를 숨기거나 성공으로 기록하지 않는다.
- 실제 실행하지 않은 명령이나 테스트를 실행한 것으로 기록하지 않는다.
- 외부 정보가 필요한 기능은 출처와 조회 시점을 확인한다.

## Documentation

- README에는 현재 실제 구현·검증 상태만 반영한다.
- 구현되지 않은 기능을 완료된 기능처럼 기록하지 않는다.
- Phase 완료 후 실제 구현·검증 결과를 `docs/results/`에 기록한다.
- 코드 주석은 짧고 직접적인 명사형 표현을 사용한다.
