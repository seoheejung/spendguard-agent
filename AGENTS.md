# AGENTS.md

> SpendGuard 저장소 공통 작업 규칙

## Scope

- 사용자의 현재 명시적 지시를 가장 우선한다.
- 기능 범위 판단이 필요할 때 `.project/plan.md`를 확인한다.
- 현재 작업 범위는 해당 Phase의 `docs/instructions/` 문서를 기준으로 제한한다.
- 현재 Phase 밖 기능, 의존성, 권한, 모델 연동, 운영 정책을 선반영하지 않는다.
- 자료에 없는 요구사항·경로·명령·결과를 만들지 않는다.
- `DESIGN.md`는 UI 기준이며 기능 범위의 최종 기준으로 사용하지 않는다.

## Work

- 필요한 파일만 확인하고 이미 확인한 내용의 재탐색을 피한다.
- 기존 코드와 문서를 먼저 확인한 뒤 최소 범위로 변경한다.
- 현재 Phase에 필요하지 않은 추상화와 공통화는 추가하지 않는다.
- 설정값과 비밀값은 환경 변수로 관리한다.
- 계산 가능한 값은 모델 판단보다 결정론적 코드 처리를 우선한다.
- 모델이 반환한 확률·confidence를 사실이나 정확도 보장으로 해석하지 않는다.
- confidence threshold는 평가 결과 없이 임의로 정하지 않는다.
- 외부 사실과 계산 결과를 임의로 생성하지 않는다.

## Model Boundaries

- OpenAI Agent는 복합 추론, Tool orchestration, 사용자용 설명에 사용한다.
- Jev는 해당 Phase에서 명시적으로 요구할 때만 좁은 typed judgment에 사용한다.
- Jev에게 금액 계산이나 최종 구매 결정을 직접 맡기지 않는다.
- Tool 실행 여부와 부작용 정책은 가능한 범위에서 코드가 소유한다.
- 모델 간 성능 차이는 고정 평가 데이터로 검증한 결과만 문서화한다.
- Jev의 Choice 후보와 판단 범위는 코드에서 명시적으로 정의하며 모델이 새로운 선택지를 생성하게 하지 않는다.
- Jev confidence threshold는 고정값을 임의로 사용하지 않고 동일 평가 데이터의 실제 결과를 근거로 결정한다.
- Jev의 낮은 confidence나 잘못된 응답을 임의의 Intent로 보정하지 않고 해당 Phase에서 정의한 fallback 또는 오류로 처리한다.
- Jev 작업 시 설치된 TypeSafe Skill과 공식 SDK·문서를 우선 확인하며 API 형태나 응답 필드를 추측해서 구현하지 않는다.

## Verification

- 변경 범위와 직접 관련된 테스트를 실행한다.
- 계산 로직은 고정 입력과 예상 결과를 기준으로 검증한다.
- 실패 결과를 숨기거나 성공으로 기록하지 않는다.
- 실제 실행하지 않은 명령이나 테스트를 실행한 것으로 기록하지 않는다.
- 외부 정보가 필요한 기능은 출처와 조회 시점을 확인한다.
- 비용·latency·정확도 비교는 같은 평가 조건을 유지한다.
- Jev 평가 시 기존 평가 데이터와 정답을 성능 개선 목적으로 변경하지 않으며 vendor benchmark와 프로젝트 실측 결과를 구분한다.

## Documentation

- README에는 현재 실제 구현·검증 상태만 반영한다.
- 구현되지 않은 기능을 완료된 기능처럼 기록하지 않는다.
- Phase 완료 후 실제 구현·검증 결과를 `docs/results/`에 기록한다.
- 계획과 실제 결과를 구분한다.
- 코드 주석은 짧고 직접적인 명사형 표현을 사용한다.
